# Claude Code guidelines

## Git workflow

- **Never push to remote automatically.** Only push when the user explicitly asks to push.
- Commits are fine when requested, but pushing is a separate, explicit action.
